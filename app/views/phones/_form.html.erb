<%= form_for @phone, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Phone</legend>

    <div class="control-group">
      <%= f.label :number, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :number, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :carrier, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :carrier, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :user_id, :class => 'control-label' %>
      <div class="controls">
        <%= f.number_field :user_id, :class => 'number_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', phones_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
